export interface User {
  id: number,
  name: string,
  surname: string,
  date_of_birth: string,
  created: string,
  updated: string,
}

export interface TitledText {
  title: string;
  text: string;
}
